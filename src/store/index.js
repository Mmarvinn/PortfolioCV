import { createStore } from 'vuex';

export default createStore({
  state() {
    return {
      todos: [],
    };
  },

  mutations: {
    // test(state, payload) {},
  },

  actions: {
    // test(context, payload) {},
  },

  getters: {
    // test(state, getters) {},
  },
});
